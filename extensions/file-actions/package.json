{
  "name": "file-actions",
  "displayName": "Damln File Actions",
  "description": "Copy content, file paths and parent folder paths from every editor title bar.",
  "version": "0.1.1",
  "publisher": "damln",
  "license": "UNLICENSED",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "extensionKind": [
    "ui"
  ],
  "capabilities": {
    "virtualWorkspaces": true,
    "untrustedWorkspaces": {
      "supported": true
    }
  },
  "main": "./extension.js",
  "files": [
    "README.md",
    "extension.js"
  ],
  "contributes": {
    "commands": [
      {
        "command": "damlnFileActions.copyContent",
        "title": "Copy content",
        "category": "File",
        "icon": "$(copy)",
        "enablement": "resource && activeCustomEditorId != damln.markdownInline"
      },
      {
        "command": "damlnFileActions.copyFilePath",
        "title": "Copy file path",
        "category": "File",
        "icon": "$(file-code)",
        "enablement": "resource && activeCustomEditorId != damln.markdownInline && resourceScheme != untitled"
      },
      {
        "command": "damlnFileActions.copyParentFolderPath",
        "title": "Copy parent folder path",
        "category": "File",
        "icon": "$(folder)",
        "enablement": "resource && activeCustomEditorId != damln.markdownInline && resourceScheme != untitled"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "damlnFileActions.copyContent",
          "when": "resource && activeCustomEditorId != damln.markdownInline",
          "group": "navigation@10"
        },
        {
          "command": "damlnFileActions.copyFilePath",
          "when": "resource && activeCustomEditorId != damln.markdownInline",
          "group": "navigation@11"
        },
        {
          "command": "damlnFileActions.copyParentFolderPath",
          "when": "resource && activeCustomEditorId != damln.markdownInline",
          "group": "navigation@12"
        }
      ],
      "commandPalette": [
        {
          "command": "damlnFileActions.copyContent",
          "when": "resource && activeCustomEditorId != damln.markdownInline"
        },
        {
          "command": "damlnFileActions.copyFilePath",
          "when": "resource && activeCustomEditorId != damln.markdownInline && resourceScheme != untitled"
        },
        {
          "command": "damlnFileActions.copyParentFolderPath",
          "when": "resource && activeCustomEditorId != damln.markdownInline && resourceScheme != untitled"
        }
      ]
    }
  }
}
